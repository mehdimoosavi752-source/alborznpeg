# نوین پلی‌تکنیک — نسخه‌ی کامل، دوزبانه، با بک‌اند واقعی

این نسخه یک **بک‌اند واقعی** و یک پنل مدیریت شبیه وردپرس دارد، و **کاملاً دوزبانه (فارسی/انگلیسی)** است:

- دیتابیس واقعی (SQLite)، رمزهای هش‌شده (bcrypt)، ورود با JWT
- **۴ نقش کاربری مثل وردپرس**: مدیر، ویرایشگر، نویسنده، مشترک
- **ویرایشگر صفحات بلوکی** برای ساخت هر تعداد صفحه‌ی دلخواه
- **کل سایت دوزبانه است** — یک دکمه‌ی «EN / فا» در هدر، هر متنی (خدمات، محصولات، منو، فوتر، سوالات رایج، صفحات) دو فیلد فارسی/انگلیسی دارد که هم در سایت و هم در پنل مدیریت قابل ویرایش است. اعداد، قیمت‌ها و تاریخ‌ها هم بر اساس زبان انتخابی فرمت می‌شوند
- محتوای پیش‌فرض با **لحن انسانی** بازنویسی شده (نه متن تبلیغاتی خشک) و شامل **۸ مقاله‌ی آموزشی واقعی** درباره‌ی نگهداری پروژکتور، راهنمای خرید، تعمیر پلی‌استیشن، SSD در برابر HDD و... است
- بخش «درگاه پرداخت» در پنل مدیریت برای وارد کردن کد پذیرندگی و کلید API (فقط مدیر می‌بیند)

ساختار پوشه‌ها:
```
novin-fullstack/
├── server/   ← بک‌اند Node.js + Express + SQLite
└── client/   ← فرانت‌اند React + Vite
```

## محدودیت شناخته‌شده درباره‌ی دوزبانگی

همه‌ی متن‌های محتوایی (چه در سایت چه در پنل مدیریت) کاملاً دوزبانه‌اند و جهت صفحه (راست‌به‌چپ/چپ‌به‌راست) با تغییر زبان عوض می‌شود. اما چیدمان بصری برای حالت انگلیسی (چپ‌به‌راست) به‌طور کامل و پیکسل‌به‌پیکسل بازطراحی نشده — یعنی ممکن است در چند نقطه‌ی جزئی (مثلاً موقعیت دقیق برخی آیکون‌ها) چیدمان صددرصد آینه‌ای نباشد، هرچند همه‌ی متن، اعداد و جهت اصلی صفحه درست کار می‌کند. اگر بخواهید این را هم کاملاً پیکسل‌به‌پیکسل تمیز کنم، بگویید تا در یک مرحله‌ی جداگانه روی همان انجامش بدهم.

## نقش‌های کاربری

| نقش | دسترسی |
|---|---|
| **مدیر (admin)** | دسترسی کامل: همه‌ی محتوا، کاربران، سفارشات، پیام‌ها، تنظیمات، درگاه پرداخت |
| **ویرایشگر (editor)** | مدیریت کامل محتوای سایت: خدمات، محصولات، منو، فوتر، هیرو، صفحات، سوالات رایج |
| **نویسنده (author)** | فقط ساخت صفحه‌ی جدید و ویرایش/حذف صفحاتی که خودش ساخته |
| **مشترک (subscriber)** | نقش پیش‌فرض هرکسی که ثبت‌نام می‌کند؛ فقط حساب کاربری و خرید |

نقش هر کاربر را از پنل مدیریت → تب «کاربران» (فقط مدیر می‌بیند) می‌توانید تغییر بدهید.

---

## ۱) اجرای محلی (برای تست قبل از دیپلوی)

### سرور
```bash
cd server
cp .env.example .env
# فایل .env را باز کنید و JWT_SECRET را با یک مقدار تصادفی طولانی جایگزین کنید
npm install
npm run dev
```
سرور روی `http://localhost:8787` بالا می‌آید و به‌صورت خودکار یک دیتابیس SQLite (`data.sqlite`) و یک حساب مدیر پیش‌فرض می‌سازد (کاربری `admin`، رمز `novin1404` — یا هرچه در `.env` گذاشتید).

### کلاینت
```bash
cd client
cp .env.example .env
npm install
npm run dev
```
حالا آدرسی که ترمینال نشان می‌دهد (معمولاً `http://localhost:5173`) را باز کنید. کلاینت به‌صورت خودکار به سرور روی `localhost:8787` وصل می‌شود.

---

## ۲) دیپلوی روی Render — فقط با یک سرویس

خبر خوب: سرور الان طوری تنظیم شده که خودش فایل‌های ساخته‌شده‌ی کلاینت را هم سرو می‌کند. یعنی دیگر لازم نیست دو سرویس جدا بسازید — همه‌چیز در **یک Web Service** بالا می‌آید.

1. کل پوشه‌ی `novin-fullstack` (شامل `client`، `server` و همین `package.json` ریشه) را در گیت‌هاب push کنید.
2. در Render: **New + → Web Service**
3. مخزن گیت‌هاب را وصل کنید
4. تنظیمات:
   - **Root Directory:** خالی بگذارید (ریشه‌ی مخزن)
   - **Build Command:** `npm run build`
   - **Start Command:** `npm start`
5. در بخش **Environment**، این متغیرها را اضافه کنید:
   - `JWT_SECRET` → یک رشته‌ی تصادفی طولانی (همین الان بسازید؛ مثلاً با اجرای `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"` روی سیستم خودتان)
   - `ADMIN_USERNAME` → مثلاً `admin`
   - `ADMIN_PASSWORD` → یک رمز قوی دلخواه
6. Create Web Service را بزنید.

همین. چون فرانت‌اند و بک‌اند روی یک آدرس (origin) سرو می‌شوند، دیگر نیازی به تنظیم `CORS_ORIGIN` یا `VITE_API_URL` هم نیست — این‌ها فقط برای زمانی هستند که بخواهید کلاینت و سرور را در دو سرویس جدا نگه دارید (مثلاً برای مقیاس‌پذیری بیشتر در آینده؛ روش قبلی هنوز هم کار می‌کند، فقط دیگر پیش‌فرض نیست).

> ⚠️ **نکته‌ی مهم درباره‌ی ماندگاری داده روی Render:**
> پلن رایگان Render فضای دیسک را بین ری‌استارت‌ها/دیپلوی‌های جدید **پاک می‌کند**، یعنی فایل `data.sqlite` (و هرچه در پنل مدیریت اضافه کرده‌اید) از بین می‌رود. برای یک فروشگاه واقعی که داده‌هایش نباید پاک شود، یکی از این دو راه را انتخاب کنید:
> - از **Render Disks** استفاده کنید (فضای دیسک دائمی، پلن پولی) و متغیر `DB_PATH` را به مسیر دیسک متصل‌شده بدهید.
> - یا دیتابیس را از SQLite به یک دیتابیس مدیریت‌شده مثل **Render PostgreSQL** (پلن رایگان هم دارد) یا **Supabase** تغییر دهید. اگر بخواهید، می‌توانم همین الان این تغییر را هم برایتان انجام بدهم تا این نگرانی هم برطرف شود.

---

## ۳) بعد از دیپلوی چه کار کنم؟

1. با حساب مدیر (`ADMIN_USERNAME` / `ADMIN_PASSWORD` که در Environment سرور گذاشتید) وارد شوید
2. حتماً **رمز عبور پیش‌فرض را عوض کنید** — فعلاً امکان تغییر رمز از پنل وجود ندارد؛ اگر لازم دارید بگویید تا این قابلیت را هم اضافه کنم
3. محتوای سایت (خدمات، محصولات، صفحات، منو، فوتر) را از پنل مدیریت ویرایش کنید

---

## نکته درباره‌ی درگاه پرداخت

از پنل مدیریت → تب «درگاه پرداخت» (فقط مدیر می‌بیند) می‌توانید نام درگاه، کد پذیرندگی (Merchant ID) و کلید API را وارد و ذخیره کنید. این اطلاعات در دیتابیس سرور ذخیره می‌شود و **هرگز** در endpoint عمومی سایت (`/api/content`) برنمی‌گردد — فقط یک وضعیت «فعال/غیرفعال» بی‌خطر به مشتری‌ها نشان داده می‌شود.

با این حال، فعلاً واقعاً به بانک وصل نمی‌شود (پرداخت هنوز شبیه‌سازی‌شده است)، چون این کار نیاز به دو مرحله‌ی اضافه دارد:
- در `server/index.js` باید دو endpoint اضافه شود: یکی برای شروع پرداخت (ارسال درخواست به درگاه و گرفتن لینک پرداخت با همان Merchant ID/API Key که در پنل ذخیره کردید) و یکی برای callback تایید پرداخت
- باید همان کد پذیرندگی که در پنل وارد کردید نزد بانک/درگاه ثبت شده باشد

وقتی آماده بودید بگویید تا این دو endpoint را هم برایتان پیاده کنم — دیگر نیازی به .env نیست، مستقیم از همان مقداری که در پنل ذخیره کرده‌اید استفاده می‌شود.

## امنیت — چه چیزهایی الان درست شده

- ✅ رمزها هش می‌شوند (bcrypt)
- ✅ نقش مدیر سمت سرور چک می‌شود، نه در کد فرانت‌اند
- ✅ توکن‌های ورود منقضی می‌شوند (۷ روز)
- ✅ endpoint های حساس (ویرایش محتوا، مشاهده‌ی کاربران/سفارشات/پیام‌ها) نیازمند نقش مدیر هستند

## چیزهایی که هنوز باقی مانده (پیشنهاد برای آینده)

- Rate limiting روی endpoint های لاگین/ثبت‌نام (جلوگیری از حمله‌ی brute-force)
- قابلیت تغییر رمز عبور و فراموشی رمز (ایمیل/پیامک بازیابی)
- اعتبارسنجی دقیق‌تر ورودی‌ها (مثلاً با Zod)
- HTTPS و دامنه‌ی اختصاصی (سرویس‌های Render/Vercel به‌صورت پیش‌فرض HTTPS رایگان می‌دهند)
- لاگ‌گیری و مانیتورینگ خطاهای سرور

## این نسخه چه چیزهایی اضافه/اصلاح شده

- 🐛 **رفع باگ:** ۸ مقاله دیگر کامل ذخیره می‌شوند (قبلاً اگر دیتابیس از قبل چند صفحه داشت، بقیه‌ی مقالات اضافه نمی‌شدند)
- 🐛 دکمه‌ی تغییر زبان (EN/فا) حالا همیشه در هدر نمایش داده می‌شود، حتی در صفحه‌نمایش‌های کوچک
- ✅ **درخواست تعمیر آنلاین**: دکمه‌ی «درخواست تعمیر» در صفحه‌ی خدمات، فرم مخصوص خودش را دارد (نوع دستگاه + توضیح مشکل)
- ✅ **گردش کار وضعیت سفارش** جدا برای خدمات (دریافت/بررسی → تکنسین‌ها مشغول‌اند → آماده تحویل → تحویل شد) و فروشگاه (در حال بررسی → ثبت شد → بسته‌بندی → ارسال → تحویل شد)، با امکان اطلاع‌رسانی ایمیل/پیامک به تایید مدیر
- ✅ **تنظیمات ایمیل (SMTP) و پیامک (Webhook عمومی)** در پنل مدیریت — فیلدها آماده‌اند، کافی است بعداً اطلاعات واقعی سرویس‌دهنده را وارد کنید
- ✅ **آپلود واقعی تصویر** برای خدمات، محصولات، و بلوک تصویر در صفحات (به‌جای فقط الگوی رنگی)
- ✅ **نظرات مشتریان**: کاربران می‌توانند برای هر محصول یا به‌صورت عمومی نظر ثبت کنند (پس از تایید مدیر نمایش داده می‌شود)؛ یک نوار نظرات هم بالای فوتر در کل سایت نمایش داده می‌شود
- ✅ **سیستم تیکت پشتیبانی**: کاربران از «حساب من» تیکت می‌سازند و پاسخ می‌بینند؛ مدیر از پنل به همه‌ی تیکت‌ها پاسخ می‌دهد
- ✅ فرم تسویه‌حساب فروشگاه حالا آدرس کامل (استان، شهر، کد پستی، آدرس دقیق، ایمیل اختیاری) می‌گیرد

## درباره‌ی سرعت سایت

اگر سایت روی پلن رایگان Render است، طبیعی است که بعد از چند دقیقه بی‌استفاده‌ماندن، سرویس «می‌خوابد» و اولین درخواست بعدی ۳۰ تا ۵۰ ثانیه طول می‌کشد تا دوباره بالا بیاید (Cold Start) — این محدودیت پلن رایگان Render است، نه مشکل کد. برای رفع کامل این موضوع باید به پلن پولی Render (Starter به بالا) ارتقا بدهید که سرویس همیشه روشن می‌ماند.

## نکته‌ی امنیتی درباره‌ی رمز ایمیل در تنظیمات اعلان‌ها

رمز SMTP و کلید API پیامک در دیتابیس ذخیره می‌شوند و فقط برای مدیر قابل مشاهده‌اند (مثل تنظیمات درگاه پرداخت). اگر از Gmail استفاده می‌کنید، به‌جای رمز اصلی حساب، حتماً یک [App Password](https://myaccount.google.com/apppasswords) بسازید.
